SELECT ?battle ?start ?end ?coord
WHERE {
  hint:Query hint:timeout "60000" .

  ?battle wdt:P31/wdt:P279* wd:Q178561.

  OPTIONAL { ?battle wdt:P580 ?start. }
  OPTIONAL { ?battle wdt:P582 ?end. }
  OPTIONAL { ?battle wdt:P625 ?coord. }
}
LIMIT 3000
